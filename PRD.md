# Codebase Navigator — MVP PRD（v0.1）

> 定位讨论的收敛稿 · 2026-09-01
> 目标：**有用户的产品（自用也算）** · 用户：**学生自学开源项目** · 节奏：**尽快迭代，写进简历找实习**

---

## 1. 定位

**一句话**：输入任意 GitHub 仓库或本地代码库，30 分钟生成一份"带你读懂它"的结构化学习报告，并支持边读边问。

**核心差异**（区别于 ChatGPT 拖文件夹问答）：
- 结构化输出：Overview / Module Map / Key Files / Roadmap
- 全部结论带 `file:line` 引用（反幻觉）
- 提供**可执行的阅读路线**，而不是零散问答

**不做（MVP 边界）**：账号/工作区/多人协作、RAG/向量库、架构图渲染、进度系统（仅勾选清单）、企业私有部署。

---

## 2. 目标用户与场景

- **人**：学生 / 自学者，想系统读懂一个开源项目
- **场景**：选了一个项目 → 想知道它整体是什么、怎么组织、先读哪里
- **环境约束**：国内网络（GitHub 不稳定）、免费或极低成本模型、单机运行

---

## 3. 成功指标

| 指标 | 目标 |
|---|---|
| 北极星：回访率 | 用户主动使用第二个仓库（自用也算） |
| 报告生成成功率 | 评测仓库 100% 无报错 |
| 幻觉率 | 关键结论无事实性错误，引用覆盖率 ≥95% |
| 上手验证 | 新用户按报告 30 分钟内答出 3 问（怎么跑 / 核心模块 / 先读哪 3 个文件） |
| 生成耗时 | 小中型仓库 < 5 分钟 |

---

## 4. MVP 功能范围（P0）

1. **仓库加载**：GitHub URL、本地路径、Gitee/镜像 URL
2. **静态分析引擎（无 LLM，秒出）**：目录结构、入口点、依赖、imports、文件热度排行
3. **LLM 结构化报告**：Overview / Module Map / Key Files / Roadmap，全部带 `file:line` 引用
4. **反幻觉约束**：只描述仓库内真实存在的内容，禁止与其他项目对比
5. **中文输出**（默认）
6. **CLI + Markdown 导出**（最便宜的交付形态）
7. **评测集 ≥10 个仓库 + 通过率脚本**（质量是 MVP 的核心，不是后补）

## 5. P1 / P2（有用户数据后再做）

- **P1**：Web 单页（报告阅读 + 内嵌 Ask 追问）、Ask AI（复用同一 agent）、大仓库分层策略、进度勾选清单
- **P2**：MCP Tool、架构图渲染（Archify 实验）、增量更新、路线个性化（基于真实完成数据）、团队 onboarding 场景
- **不做**：Repo RAG（用工具 + 长上下文替代，参考项目已验证）

---

## 6. 评测集清单（起步 10 个）

覆盖语言、规模、复杂度的混合，含中文项目：

| # | 仓库 | 语言 | 规模 | 看点 |
|---|---|---|---|---|
| 1 | `psf/requests` | Python | 中 | 经典库，结构清晰 |
| 2 | `psf/requests` | Python | 中 | 经典库，结构清晰 |
| 3 | `pallets/flask` | Python | 小 | 极简框架，易验证 |
| 4 | `gradio-app/gradio` | Python | 大 | LLM 工具 UI，热点项目 |
| 5 | `textualize/rich` | Python | 中 | 终端渲染库 |
| 6 | `vitejs/vite` | TypeScript | 中大 | 前端工具链，monorepo |
| 7 | `gin-gonic/gin` | Go | 小 | Go Web，结构简单 |
| 8 | `sharkdp/fd` | Rust | 小 | 小工具，源码易读 |
| 9 | `ruoyi-vue`（若依） | Java+Vue | 中大 | 中文前后端业务项目 |
| 10 | `facebook/react` | TypeScript | 大 | 压测项：大仓库策略 |

> 规则：评测时只允许工具探索，人工核对每个结论是否有 `file:line` 依据。若某仓库在当前网络下克隆失败，替换为可访问的 Trending 仓库即可。

---

## 7. 里程碑（6 周，每天大部分时间投入）

| 周 | 目标 | 简历可用素材 |
|---|---|---|
| W1 | 项目骨架 + 静态分析引擎 + 评测集搭建 | 确定性分析引擎（目录/入口/依赖/imports/热度） |
| W2 | LLM 报告生成 + 引用 + 反幻觉评测 | Agent 工具调用循环 + 评测通过率 |
| W3 | CLI + Markdown 导出，10 仓库跑通 | 端到端 Demo |
| W4 | Web 单页 + Ask 追问 | 产品化形态 |
| W5 | 打磨质量 + 内测（自用/朋友）+ 公开 | 真实用户反馈数据 |
| W6 | 学习闭环（勾选清单）+ 整理简历项目描述 | 完整项目故事 |

---

## 8. 技术决策（推荐，待确认）

- **语言**：Python 3.12（迭代快、LLM 生态好、与参考项目一致）
- **Agent 实现**：自研工具调用循环（openai SDK + OpenAI 兼容 API），不依赖 LangChain/LangGraph —— 学习价值更高、简历更好讲
- **模型**：默认 DeepSeek（国内直连、便宜），OpenRouter 免费档备选，支持自定义 base_url（可接本地 Ollama）
- **静态分析**：纯 Python 实现（不依赖 rg/grep，跨平台）
- **界面**：CLI（argparse）→ Gradio 单页
- **数据**：无数据库，报告输出 Markdown + JSON
- **参考项目**：借鉴工具设计与评测方法，代码自己写

---

## 9. 下一步（待你确认）

1. 确认第 8 节技术决策
2. 确认评测集清单（可增删）
3. 批准后开始 **Phase 1：项目骨架 + 静态分析引擎**
